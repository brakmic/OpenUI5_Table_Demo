/* this version shows a Master-Detail demo by using the remote "Northwind" OData Service*/
requirejs(['advarics/app'], function (app) {
    console.log('Executing main.js');
    //set helpers for console
    app.setHelpers();
    app.altInit(); 
});